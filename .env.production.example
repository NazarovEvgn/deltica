# Docker Production Environment Configuration
# Скопируйте этот файл в .env.production и заполните реальными значениями

# Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_NAME=deltica_prod
DB_USER=deltica_user
DB_PASSWORD=CHANGE_THIS_STRONG_PASSWORD_MIN_16_CHARS

# Backend Configuration
SECRET_KEY=CHANGE_THIS_SECRET_KEY_MINIMUM_32_CHARACTERS_RANDOM_STRING

# Примеры генерации безопасных значений:
#
# Для DB_PASSWORD (PowerShell):
# -r -Count 1 -Minimum 100000000000000 -Maximum 999999999999999 | ForEach-Object { [Convert]::ToBase64String([System.Text.Encoding]::UTF8.GetBytes($_)) }
#
# Для SECRET_KEY (PowerShell):
# [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
#
# Или используйте онлайн генераторы:
# https://randomkeygen.com/ (выберите "Fort Knox Passwords")
