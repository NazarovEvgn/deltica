═══════════════════════════════════════════════════════════════════
  ЧТО БРАТЬ НА ФЛЕШКУ ДЛЯ РАЗВЕРТЫВАНИЯ НА ДРУГОМ КОМПЬЮТЕРЕ
═══════════════════════════════════════════════════════════════════

📌 КРАТКИЙ ОТВЕТ:

Копируйте ВСЮ папку C:\Projects\deltica на флешку,
НО УДАЛИТЕ эти тяжелые папки:

  ❌ frontend/node_modules      (~500 MB)
  ❌ frontend/dist               (~10 MB)
  ❌ frontend/dist-electron      (~700 MB)
  ❌ backend/__pycache__         (~1 MB)
  ❌ backend/.venv (или venv)    (~200 MB)
  ❌ backend/uploads             (опционально - ваши файлы)
  ❌ backend/backups             (опционально)
  ❌ backend/logs                (опционально)

ИТОГО: на флешку поместится ~20-50 MB вместо 1.5 GB


═══════════════════════════════════════════════════════════════════
  АВТОМАТИЧЕСКОЕ КОПИРОВАНИЕ (РЕКОМЕНДУЕТСЯ)
═══════════════════════════════════════════════════════════════════

PowerShell команда (замените E: на букву вашей флешки):

robocopy "C:\Projects\deltica" "E:\deltica-deploy" /E /XD node_modules dist dist-electron __pycache__ .venv venv .git uploads backups logs generated_documents /XF *.pyc *.log


═══════════════════════════════════════════════════════════════════
  НА НОВОМ КОМПЬЮТЕРЕ - БЫСТРАЯ УСТАНОВКА
═══════════════════════════════════════════════════════════════════

1. УСТАНОВИТЕ ПО:
   - Python 3.13 (https://www.python.org/downloads/)
   - Node.js 20.19+ (https://nodejs.org/)
   - PostgreSQL 13+ (https://www.postgresql.org/download/windows/)
   - uv (выполните: powershell -c "irm https://astral.sh/uv/install.ps1 | iex")

2. СКОПИРУЙТЕ проект с флешки:
   Copy-Item E:\deltica-deploy C:\Projects\deltica -Recurse

3. НАСТРОЙТЕ .env:
   cd C:\Projects\deltica
   copy .env.example .env
   notepad .env    # Отредактируйте DB_USER, DB_PASSWORD, DB_HOST

4. УСТАНОВИТЕ ЗАВИСИМОСТИ:
   uv sync
   cd frontend && npm install && cd ..

5. НАСТРОЙТЕ БД (через pgAdmin или psql):
   CREATE DATABASE deltica_db;
   CREATE USER deltica_user WITH PASSWORD 'your_password';
   GRANT ALL PRIVILEGES ON DATABASE deltica_db TO deltica_user;

6. МИГРАЦИИ И ПОЛЬЗОВАТЕЛИ:
   uv run alembic upgrade head
   uv run python backend/scripts/seed_users.py

7. ЗАПУСТИТЕ:
   .\start.ps1

8. ОТКРОЙТЕ:
   http://localhost:5173
   Логин: admin / Пароль: admin123


═══════════════════════════════════════════════════════════════════
  ВАЖНО!
═══════════════════════════════════════════════════════════════════

✅ НА ФЛЕШКУ:
   - Исходный код проекта (backend/, frontend/src/, config/, docs/)
   - Конфигурационные файлы (package.json, pyproject.toml, .env.example)
   - Скрипты запуска (start.ps1, start.bat)
   - Документацию (CLAUDE.md, DEPLOYMENT_GUIDE.md)

❌ НЕ БРАТЬ (установятся автоматически):
   - node_modules (frontend/node_modules) - 500 MB
   - .venv (backend/.venv) - 200 MB
   - dist папки - 700+ MB

🔑 ФАЙЛЫ, КОТОРЫЕ ТОЧНО НУЖНЫ:
   backend/pyproject.toml
   backend/uv.lock
   backend/alembic.ini
   backend/alembic/
   backend/app/
   backend/core/
   backend/routes/
   backend/services/
   backend/scripts/
   frontend/package.json
   frontend/package-lock.json
   frontend/src/
   frontend/public/
   frontend/electron/
   frontend/vite.config.js
   config/
   .env.example
   start.ps1
   start.bat


═══════════════════════════════════════════════════════════════════
  АЛЬТЕРНАТИВНЫЙ ВАРИАНТ - ЧЕРЕЗ GIT
═══════════════════════════════════════════════════════════════════

ПРОЩЕ И БЫСТРЕЕ:

1. На исходном компьютере:
   git push origin main

2. На новом компьютере:
   git clone https://github.com/NazarovEvgn/deltica.git
   cd deltica

3. Далее - пункты 3-8 из инструкции выше


═══════════════════════════════════════════════════════════════════

📖 Подробная инструкция: DEPLOYMENT_GUIDE.md

═══════════════════════════════════════════════════════════════════
